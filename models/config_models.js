const definicao_mapeamento = require('./definicao_mapeamento');
const definicao_url = require('./definicao_url');

module.exports = {
    definicao_mapeamento : definicao_mapeamento,
    definicao_url : definicao_url
}